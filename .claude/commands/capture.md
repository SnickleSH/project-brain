---
description: Append a quick raw note to the inbox
---

Note content: $ARGUMENTS

Create `brain/0-inbox/<today's date YYYY-MM-DD>-<3-5 word slug>.md` using
`brain/_templates/note.md`. Paste my content verbatim into the body — do not
summarize, rewrite, "improve", or expand it. You may add up to three
`[[wikilinks]]` at the bottom under `Related:` if the note obviously connects
to existing architecture docs.

If a note with the same date and a clearly identical topic already exists,
append to it under a `---` divider instead of creating a duplicate.

Print only the file path. No commentary.
